import { DateTimeFormatFilterPipe } from './date-time-format-filter.pipe';

describe('DateTimeFormatFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeFormatFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
